import React from "react";

const About = props => {
  return (
    <React.Fragment>
      <div>
        <h4>
          
        </h4>

        <button onClick={props.switchPage}>Back home</button>

      </div>
    </React.Fragment>

  );
};

export default About;
